import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xff3a7bc8);
  static const dangerColor = Colors.red;
  static final discardColor = Colors.grey[400];
  static const secondaryColor = Colors.green;
  static final subtitleColor = Colors.grey;
  static final surfaceColor = Colors.grey[100];
  static const verifiedColor = Colors.green;
}
